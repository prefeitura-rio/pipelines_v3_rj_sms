name: Register flows (prod)

# on:
#   push:
#     branches:
#       - master
#     paths:
#       - pipelines/**

jobs:
  runs-on: ubuntu-latest
  timeout-minutes: 30

  steps:
    - name: Checkout repository
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    # fixme
    # - uses: actions/cache@v5
    #   with:
    #     path: ~/.cache/pip
    #     key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
    #     restore-keys: |
    #       ${{ runner.os }}-pip-

    # fixme
    # - name: Tailscale
    #   uses: tailscale/github-action@v4
    #   with:
    #     oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
    #     oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
    #     tags: tag:prefect-cicd

    # - name: Log in to GitHub container registry
    #   uses: docker/login-action@v3
    #   with:
    #     registry: ghcr.io
    #     username: ${{ github.actor }}
    #     password: ${{ secrets.GH_TOKEN }}

    # - name: Setup Python
    #   uses: actions/setup-python@v5
    #   with:
    #     python-version-file: pyproject.toml

    # - name: Install uv
    #   shell: bash
    #   run: pip install uv

    # - name: Deploy Prefect flows
    #   shell: bash
    #   env:
    #     ENVIRONMENT: prod
    #     PREFECT_API_URL: https://prefect.squirrel-regulus.ts.net/api
    #     PIPELINES_PATH: ./pipelines
    #     FORCE_DEPLOY: ${{ github.event.inputs.force_deploy == 'true' && '1' || '0' }}
    #     GITHUB_SHA: ${{ github.sha }}
    #     LOG_LEVEL: ${{ github.event.inputs.debug == 'true' && 'DEBUG' || 'INFO' }}
    #     BATCH_SIZE: ${{ github.event.inputs.batch_size || '3' }}
    #     CLEANUP_DOCKER: ${{ (github.event.inputs.cleanup_docker || 'true') == 'true' && '1' || '0' }}
    #     MAX_RETRIES: ${{ github.event.inputs.max_retries || '2' }}
    #     PIPELINE: ${{ github.event.inputs.pipeline }}
    #   run: uv run .github/scripts/deploy_prefect_flows.py
